export type TSchema = {
    id?: string
    title: string
    version: string
    description: string
    summary: string
    required: string[]
    properties: object
    archive: object
    source: string
    updated: string;
    created: string;
}
